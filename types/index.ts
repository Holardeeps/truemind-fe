export interface Inventory {
  name: string;
  price: string;
  description: string;
  img: string;
}

export interface Select {
  id: string;
  price: number;
  name: string;
  isDefault: boolean;
}
